# tapclicks-assignment

## How to run-
- use `yarn install` to install dependencies
- add .env file and add these envs
`
DB_HOST=localhost
DB_NAME=****
DB_USER=*****
DB_PASSWORD=****
`
- use  `yarn dev` to run
